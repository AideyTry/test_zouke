import loading from '@local/common/ui/loading/weix';
import toast from '@local/common/ui/toast/weix';
import { DEBUG } from '../../config';

const loginPage = 'login'; // 登录route name
const timeout = 10000; //ajax timeout

export { 
    loading, 
    toast, 
    DEBUG,
    loginPage,
    timeout
};